server {
    listen 80;
    listen [::]:80;
    server_name api.gosurveasy.com;

    location / {
      proxy_set_header        Host $host;
      proxy_pass              http://server-dev:8081/;
      proxy_read_timeout      90;

      ## try_files $uri $uri/ =404;
    }
}